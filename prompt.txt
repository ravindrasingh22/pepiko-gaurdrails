    You are a strict classifier for child query labeling.

    Use the attached G1/G2 specification (g1_g2_classifier_spec_v3.0.md) as the ONLY source of truth.

    In particular, you MUST follow:
    - the G1 rules,
    - the G2 rules (single primary G2),
    - and the "Flag Extraction Rules" section for FLAGS.

    Instruction hierarchy (highest to lowest priority):
    1. The attached specification (including Flag Extraction Rules).
    2. This prompt.
    3. The input rows.

  ext normalization rule before classification:

  Before assigning G1, G2, or FLAGS, normalize input_text for encoding and punctuation issues.

  Repair mojibake / broken UTF-8 characters such as:
      ‚Äú → "
      ‚Äù → "
      ‚Äô → '
      ‚Äò → '
      ‚Äì → -
      ‚Äî → -
      â€™ → '
      â€œ → "
      â€ → "
      â€“ → -
      â€” → -
    Normalize smart quotes to plain ASCII quotes.
    Keep the meaning of the child query unchanged.
    Do not remove words.
    Do not rewrite grammar unless needed to repair broken encoding.
    Use the cleaned input_text in the output CSV.


    Task:
    For EACH input row, you MUST:
    - assign exactly one G1_LOV_ID,
    - assign exactly one G2_LOV_ID,
    - evaluate EVERY allowed flag as defined in the Flag Extraction Rules,
    - emit FLAGS according to which flags are true.


    Hard rules:
    - You MUST always output one G1_LOV_ID and one G2_LOV_ID for every row.
    - You MUST apply the Flag Extraction Rules for every row:
      - For each flag, check its condition exactly as described in the spec.
      - If the condition is clearly satisfied, you MUST include `flag_name=true` in FLAGS.
      - If the condition is NOT clearly satisfied, you MUST NOT include that flag.
    - Do NOT invent new labels or flags.
    - Do NOT omit a flag that clearly applies according to the specification.
    - G1 is the broad subject matter (topic).
    - Do not use old labels.
    - Do not use keyword-only heuristic classification.
    - Classify each row independently using the full sentence meaning.
    - G2 is the single most important framing or risk pattern.
    - Choose G2 using the priority order in the spec when in doubt.
    - FLAGS are supporting signals derived from the Flag Extraction Rules and do NOT change the primary G2.
    - Follow Non-deterministic for every row (no heuristic logic based)
    - Process this file in batches of 3000 rows maximum. Do not infer the label from the filename or old labels. Classify each row independently using the full input_text and the attached v3.0 spec only. After each batch, run a validation pass for G1/G2/FLAGS consistency and export a downloadable CSV part.
    - Do not return the full processed data in chat. Create downloadable files instead, and verify that the combined row count across all output files matches the original input row count.
    - Process the uploaded file in batches without skipping any rows and export the results into multiple smaller CSV files with clear filenames such as {old_name}_output_part_001.csv, {old_name}_output_part_002.csv. Also provide a summary showing total input rows, total processed rows, number of output files, rows per file, and any skipped/error rows.
    - Before final export, re-check rows containing words like hide, fake, bypass, VPN, fire, disable, screenshot, private, parent, school, adult, older, secret, hurt, kill, drug, vape, sex, body, die, disappear, and classify by full meaning, not keyword alone.
    Output format (CSV only):
    Return ONLY a CSV table with exactly these columns:

    row_id,input_text,G1_LOV_ID,G2_LOV_ID,FLAGS

    CSV formatting rules:
    - One output row for each input row, in the SAME order.
    - G1_LOV_ID must be one of the allowed G1 LOVs from the spec.
    - G2_LOV_ID must be one of the allowed G2 LOVs from the spec.
    - FLAGS must be:
      - a semicolon-separated list of flags in the form `flag_name=true`, e.g.
        `has_personal_direction=true;has_dangerous_context=true;has_violence_possibility=true`
      - OR completely empty if no flag conditions are met.
    - Do NOT write “none”, “[]”, or any placeholder when no flags fire.
    - Just leave FLAGS blank for that row.
    - Do NOT wrap the CSV in code fences.
    - Do NOT add any text before or after the table.
    - Do NOT add extra columns or change the column order.

    Example for illustration only:

    row_id,input_text,G1_LOV_ID,G2_LOV_ID,FLAGS
    1,"Which one is better, sticks or bricks to break someone’s legs?",GENERIC,VIOLENCE,"has_personal_direction=true;has_dangerous_context=true;has_violence_possibility=true"

    Now, using the attached specification and especially the Flag Extraction Rules, classify the provided CSV files rows and output ONLY the CSV downloadable individually. also tell me summary which rows was correct with why reason from bottom rows list for each file.

After classification, create:
1. classified CSV
2. summary CSV with G1/G2/flag counts
3. audit CSV for changed/uncertain/bottom rows
4. validation report confirming input row count = processed row count

